NAME=xfce4-notes-plugin
VERSION=0.10.0
ARCH=i686
RELEASE=1
URL="http://www.xfce.org/"
DESC="A notes applet for the XFce panel."
GROUP=xfce-extra
LICENSE=GPLv2
SIZE="319K"
HUSE="static debug profile"

SOURCES="http://download.berlios.de/xfce-goodies/xfce4-notes-plugin-0.10.0.tar.gz"

BUILDTIME="pkgconfig"

RUNTIME=">=xfce4-panel-4.1.90"

build() {
  cd $SRC/$NAME-$VERSION

  use static && OPTS="--enable-static" || OPTS="--disable-static"
  use debug && OPTS="$OPTS --enable-debug=full"
  use profile && OPTS="$OPTS --enable-profiling"
      
  ./configure --prefix=/opt/xfce \
              --sysconfdir=/etc/opt/xfce \
	      $OPTS
  make
  make DESTDIR=$PKG install
  
  install_docs AUTHORS COPYING ChangeLog INSTALL NEWS README
}


