NAME=xfce4-systemload-plugin
VERSION=0.3.6
ARCH=i686
RELEASE=1
URL="http://www.xfce.org/"
DESC="XFce4 system load panel plugin."
GROUP=xfce-extra
LICENSE=BSDL
SIZE="224K"
HUSE="static debug profile"

SOURCES="http://download.berlios.de/xfce-goodies/xfce4-systemload-plugin-0.3.6.tar.gz"

BUILDTIME="gettext pkgconfig"

RUNTIME=">=xfce4-panel-3.99.2"

build() {
  cd $SRC/$NAME-$VERSION

  use static && OPTS="--enable-static" || OPTS="--disable-static"
  use debug && OPTS="$OPTS --enable-debug=full"
  use profile && OPTS="$OPTS --enable-profiling"
  
  ./configure --prefix=/opt/xfce \
              --sysconfdir=/etc/opt/xfce \
	      $OPTS
  make
  make DESTDIR=$PKG install
  
  install_docs AUTHORS COPYING ChangeLog INSTALL NEWS README
}


