NAME=libgksuui
VERSION=1.0.3
ARCH=i686
RELEASE=1
URL="http://www.nongnu.org/gksu/index.html"
DESC="A gksy authorization library (GUI)."
GROUP=gnome-extra
LICENSE="GPLv2"
SIZE="540K"
HUSE="static nls gtkdoc"

SOURCES="http://people.debian.org/~kov/gksu/libgksuui1.0/libgksuui1.0-1.0.3.tar.gz"

RUNTIME="gtk2 xorg"

use nls BUIDLTIME="gettext"
use gtkdoc BUILDTIME="$BUILDTIME gtk-doc"

build() {
  cd $SRC/${NAME}1.0-$VERSION
  
  use static && OPTS="--enable-static" || OPTS="--disable-static"
  use nls || OPTS="$OPTS --disable-nls"
  use gtkdoc || OPTS="$OPTS --disable-gtk-doc"
  
  ./configure --prefix=/opt/gnome \
              $OPTS
  make
  make DESTDIR=$PKG install
  
  install_docs ABOUT-NLS AUTHORS COPYING ChangeLog INSTALL
}
