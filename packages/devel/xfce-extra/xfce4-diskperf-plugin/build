NAME=xfce4-diskperf-plugin
VERSION=1.5
ARCH=i686
RELEASE=1
URL="http://www.xfce.org/"
DESC="Disk performance XFce4 panel plugin (DiskPerf)."
GROUP=xfce-extra
LICENSE=BSDL
SIZE="221K"
HUSE="static debug profile"

SOURCES="http://download.berlios.de/xfce-goodies/xfce4-diskperf-plugin-1.5.tar.gz"

BUILDTIME="pkgconfig"

RUNTIME=">=xfce4-panel-4.0.0"

build() {
  cd $SRC/$NAME-$VERSION
  
  use static && OPTS="--enable-static" || OPTS="--disable-static"
  use debug && OPTS="$OPTS --enable-debug=full"
  use profile && OPTS="$OPTS --enable-profiling"
	
  ./configure --prefix=/opt/xfce \
              --sysconfdir=/etc/opt/xfce \
	      $OPTS
  make
  make DESTDIR=$PKG install
  
  install_docs AUTHORS COPYING ChangeLog INSTALL NEWS README
}


