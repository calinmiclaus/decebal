#!/bin/bash
#
# /etc/rc.d/kadmind - control file for the Heimdal Admin daemon
#

. /etc/rc.d/functions

case "$1" in 
  start)
    msg "Starting the Heimdal Admin daemon"
    start_service /usr/sbin/kadmind
    ;;
  stop)
    msg "Stopping the Heimdal Admin daemon"
    stop_service /usr/sbin/kadmind
    ;;
  restart|force-reload)
    $0 stop
    sleep 1
    $0 start
    ;;
  status)
    status_service /usr/sbin/kadmind
    ;;
  *)
    echo "usage: $0 {start|stop|force-reload|restart|status}"
    exit 1
esac

